@model CinemaManager.Models.Show

@{
    ViewData["Title"] = "Details";
}

<h1>Book a ticket</h1>

<div>
    <h4>Show</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Film.Title)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Film.Title)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Genre)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Genre)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Price)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Price)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Hall.Nr)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Hall.Nr)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Hall.Rows)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Hall.Rows)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Hall.Columns)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Hall.Columns)
        </dd>

    </dl>
</div>
<div asp-validation-summary="ModelOnly" class="text-warning"></div>
@for (int i = 0; i < Model.Hall.Rows; i++)
{
    //string selectedSeat = TempData["selectedSeat"].ToString();

    for (int j = 0; j < Model.Hall.Columns; j++)
    {
        if (TempData["row"] != null && TempData["column"] != null)
        {
            if (i == (int)TempData["row"] && j == (int)TempData["column"])
            {<a asp-route-row="@i" asp-route-column="@j" class="btn btn-warning" role="button" style="width:40px" asp-action="Details">@(i * Model.Hall.Rows + j + 1)</a> }
            else
            {<a asp-route-row="@i" asp-route-column="@j" class="btn btn-outline-primary" role="button" style="width:40px" asp-action="Details">@(i * Model.Hall.Rows + j + 1)</a> }
        }
        else
        {
            {<a asp-route-row="@i" asp-route-column="@j" class="btn btn-outline-primary" role="button" style="width:40px" asp-action="Details">@(i * Model.Hall.Rows + j + 1)</a> }
        }


    }
    <br />
}
<div>
    @*<a asp-action="Edit" asp-route-id="@Model.Id">Edit</a> |*@
    <a asp-action="Edit" asp-route-id="@Model.Id">Confirm</a> |
    <a asp-action="Index">Back to List</a>
</div>
